#!/usr/bin/env perl

use IPC::Run qw/run/;
use List::MoreUtils qw/all any none uniq/;
use Data::Dumper;
no if ($] >= 5.018), 'warnings' => 'experimental::smartmatch';

$HOME = $ENV{'HOME'};

sub list_ssh_known_hosts
{
	my $input = '';
	my $output = '';
	
	run ['bash', '-c', '
			. /usr/share/bash-completion/bash_completion;
			_known_hosts_real "";
			printf "%s\n" "${COMPREPLY[@]}";
		'], \$input, \$output;
	
	chomp $output;
	return split /\n/, $output;
}

sub list_ssh_config_hosts
{
	my @hosts;
	open my $fh, '<', "$HOME/.ssh/config";
	while(<$fh>)
	{
		chomp;
		if(/^\s*Host\s+(.+)/i)
		{
			my $host_patterns = $1;
			for my $hp (split /\s+/, $host_patterns)
			{
				$hp =~ s/^!//;
				push @hosts, $hp, if $hp !~ /[\*\?]/;
			}
		}
	}
	close $fh;
	return @hosts;
}

sub ssh_control_sockets
{
	my $prefix = shift;
	my @names;
	open my $fh, '<', '/proc/net/unix';
	while(<$fh>)
	{
		chomp;
		my ($sockname) = /^(?:\S+\s+){7}(.+)$/;
		if($prefix eq substr $sockname, 0, length $prefix)
		{
			my $suffix = substr $sockname, length $prefix;
			push @names, $suffix =~ s/\.[[:alnum:]]+$//r;
		}
	}
	close $fh;
	return @names;
}

@known_hosts = list_ssh_known_hosts;
@config_hosts = list_ssh_config_hosts;
@rhosts = sort {lc $a cmp lc $b} uniq @known_hosts, @config_hosts;

# Note, have to set this ssh config:
# ControlPath ~/.ssh/ctrl/%r@%n(%h):%p
@connected_hosts = map { /\@(.+?)\(/; $1; } ssh_control_sockets("$HOME/.ssh/ctrl/");

sub command_executor
{
	my $title = shift;
	my $cmdref = shift;
	my @cmd = @$cmdref;
	return "x-terminal-emulator -T \"$title\" -e env REPEAT_UNTIL=0 REPEAT_DELAY=enter repeat " . join(' ', map {/^\w+$/ ? $_ : "\"$_\""} @cmd);
}

for my $rhost (@rhosts)
{
	my $icon = "laptopdisconnected";
	if($rhost ~~ @connected_hosts)
	{
		$icon = "laptopconnected";
	}
	print "prog \"$rhost\" $icon " . command_executor("ssh $rhost", ["ssh", $rhost]) . "\n";
	if($rhost ~~ @connected_hosts)
	{
		print "prog \"$rhost DISCONNECT\" gtk-disconnect " . command_executor("disconnect ssh: $rhost", ["ssh", $rhost, "-O", "exit"]) . "\n";
	}
}
