
#ifndef _INCLUDE_LIBMALLOCAB_H
#define _INCLUDE_LIBMALLOCAB_H

void* mallocab(size_t);
void* reallocab(void*, size_t);
void* reallocab(void*, size_t);
char* strdupab(const char*);
char* strndupab(const char*, size_t);

#endif
